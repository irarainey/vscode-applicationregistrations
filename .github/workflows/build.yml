name: Lint, Build & Test

on: 
  workflow_dispatch:
  pull_request:
    branches: [main]

jobs:
  lintBuildTest:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: NPM Install
      run: npm install

    - name: Run tests
      run: npm run test